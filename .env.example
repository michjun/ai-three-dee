# Do not share your OpenAI API key with anyone! It should remain a secret.
OPENAI_API_KEY=
MONGODB_URI＝
MONGODB_DB=
NEXT_PUBLIC_HOST_URL=
# 0: no shot mode, 1: dynamic mode, 2: dynamic mode, 3: chain of thought
# If not specified, will pick a random mode
FEW_SHOT_MODE=